# Quantax: Arrays with Quantities in JAX

## Installation

You can install Quantax simply via 

```bash
pip install quantax
```
We recommend installing also the GPU-acceleration from JAX, which will massively increase speed:
```bash
pip install jax[cuda]
```